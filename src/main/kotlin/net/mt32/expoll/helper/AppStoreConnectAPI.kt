package net.mt32.expoll.helper

object AppStoreConnectAPI {
    /*fun generateJWT(): String{
        val scopes = listOf("")
        val iss = ""
        val kid = ""
        JWT.create()
            .withKeyId(kid)

            .withAudience("appstoreconnect-v1")
            .withIssuer(iss)
            .withExpiresAt(UnixTimestamp.now().addMinutes(20).toDate())
            .withIssuedAt(UnixTimestamp.now().toDate())
            .withClaim("scope", scopes)
            .sign(Algorithm.ECDSA256())

    }*/
}